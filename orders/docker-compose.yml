version: '3'
services:
  app:
    build:
      context: .
      args:
        BUILDPLATFORM: linux/amd64
    platform: linux/amd64
    ports:
      - "3002:3002"
    env_file:
      - .env
    volumes:
      - .:/app
      - /app/node_modules
